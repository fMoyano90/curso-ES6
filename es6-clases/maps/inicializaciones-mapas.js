// Para inicializar los mapas por defecto lo debemos hacer en un arreglo
// dentro del arreglo debemos agregar los pares de valores en otros arreglos
// Se puede meter lo que sea dentro de un mapa

let mapa = new Map([
  ["nombre", "Felipe"],
  ["edad", 29]
]);

console.log(mapa);
